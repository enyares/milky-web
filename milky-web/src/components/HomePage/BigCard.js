import { Card } from "@mui/material";
import React from "react";

const BigCard = () => {
  return (
    <Card
      sx={{
        width: "auto",
        height: "744px",
        borderRadius: 2,
        backgroundColor: "black",
      }}
    >
      BigCard
    </Card>
  );
};

export default BigCard;
